import 'package:flutter/material.dart';

class TableWidgets extends StatelessWidget {
  const TableWidgets({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
